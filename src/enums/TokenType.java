package enums;

public enum TokenType {
    NUMBER,
    IDENTIFIER,
    COMMENT,
    SEMICOLON,      // ;
    EQUALS,         // =
    PLUS,           // +
    MULTIPLY,       // *
    LEFT_PAREN,     // (
    RIGHT_PAREN,    // )
    END_OF_TEXT,
    UNKNOW
}
